{:format :kotoba.kir/v4,
 :signature nil,
 :functions
 [{:name token-secret-name,
   :params [],
   :result :string,
   :effects #{},
   :body "murakumo-token",
   :param-types []}
  {:name token-secret-env,
   :params [],
   :result :string,
   :effects #{},
   :body "MURAKUMO_TOKEN_SECRET",
   :param-types []}
  {:name service-token-name,
   :params [],
   :result :string,
   :effects #{},
   :body "murakumo-service-token",
   :param-types []}
  {:name service-token-env,
   :params [],
   :result :string,
   :effects #{},
   :body "MURAKUMO_SERVICE_TOKEN",
   :param-types []}
  {:name metrics-token-name,
   :params [],
   :result :string,
   :effects #{},
   :body "murakumo-metrics-token",
   :param-types []}
  {:name metrics-token-env,
   :params [],
   :result :string,
   :effects #{},
   :body "MURAKUMO_METRICS_TOKEN",
   :param-types []}
  {:name quic-cert-path-name,
   :params [],
   :result :string,
   :effects #{},
   :body "murakumo-quic-cert-path",
   :param-types []}
  {:name quic-cert-path-env,
   :params [],
   :result :string,
   :effects #{},
   :body "MURAKUMO_QUIC_CERT",
   :param-types []}
  {:name quic-key-path-name,
   :params [],
   :result :string,
   :effects #{},
   :body "murakumo-quic-key-path",
   :param-types []}
  {:name quic-key-path-env,
   :params [],
   :result :string,
   :effects #{},
   :body "MURAKUMO_QUIC_KEY",
   :param-types []}
  {:name max-env-name,
   :params [],
   :result :i64,
   :effects #{},
   :body 256,
   :param-types []}
  {:name max-path-ref,
   :params [],
   :result :i64,
   :effects #{},
   :body 1024,
   :param-types []}
  {:name class-value,
   :params [],
   :result :string,
   :effects #{},
   :body "value",
   :param-types []}
  {:name class-not-found,
   :params [],
   :result :string,
   :effects #{},
   :body "not-found",
   :param-types []}
  {:name class-empty,
   :params [],
   :result :string,
   :effects #{},
   :body "empty",
   :param-types []}
  {:name class-fetch,
   :params [],
   :result :string,
   :effects #{},
   :body "fetch",
   :param-types []}
  {:name class-unknown,
   :params [],
   :result :string,
   :effects #{},
   :body "unknown",
   :param-types []}
  {:name error-code-prefix,
   :params [],
   :result :string,
   :effects #{},
   :body "secret/",
   :param-types []}
  {:name msg-empty,
   :params [],
   :result :string,
   :effects #{},
   :body "empty",
   :param-types []}
  {:name msg-not-found,
   :params [],
   :result :string,
   :effects #{},
   :body "not found",
   :param-types []}
  {:name msg-fetch,
   :params [],
   :result :string,
   :effects #{},
   :body "getter failed",
   :param-types []}
  {:name msg-unknown,
   :params [],
   :result :string,
   :effects #{},
   :body "unknown",
   :param-types []}
  {:name pem-begin-marker,
   :params [],
   :result :string,
   :effects #{},
   :body "-----BEGIN",
   :param-types []}
  {:name ws?,
   :params [c],
   :result :bool,
   :effects #{},
   :body
   (let
    [__kotoba_or_4 (string=? c " ")]
    (if
     __kotoba_or_4
     __kotoba_or_4
     (let
      [__kotoba_or_3 (string=? c "\n")]
      (if
       __kotoba_or_3
       __kotoba_or_3
       (let
        [__kotoba_or_2 (string=? c "\t")]
        (if __kotoba_or_2 __kotoba_or_2 (string=? c "\r"))))))),
   :param-types [:string]}
  {:name blank?,
   :params [s],
   :result :bool,
   :effects #{},
   :body
   (let
    [n (string-byte-length s)]
    (if
     (= n 0)
     true
     (if
      (ws? (string-substring s 0 1))
      (blank? (string-substring s 1 n))
      false))),
   :param-types [:string]}
  {:name valid-env-var-name?,
   :params [env-name],
   :result :bool,
   :effects #{},
   :body
   (if
    (blank? env-name)
    false
    (if
     (< (max-env-name) (string-byte-length env-name))
     false
     (if
      (string-contains? env-name "*")
      false
      (if
       (string-contains? env-name "/")
       false
       (if
        (string-contains? env-name "\\")
        false
        (if (string-contains? env-name " ") false true)))))),
   :param-types [:string]}
  {:name valid-path-ref-unix?,
   :params [p],
   :result :bool,
   :effects #{},
   :body
   (if
    (blank? p)
    false
    (if
     (< (max-path-ref) (string-byte-length p))
     false
     (if
      (string-contains? p "\u0000")
      false
      (if
       (string-contains? p (pem-begin-marker))
       false
       (if
        (string-contains? p "*")
        false
        (let
         [n (string-byte-length p)]
         (if
          (< n 1)
          false
          (string=? (string-substring p 0 1) "/")))))))),
   :param-types [:string]}
  {:name known-secret-name?,
   :params [name],
   :result :bool,
   :effects #{},
   :body
   (let
    [__kotoba_or_5 (string=? name (token-secret-name))]
    (if
     __kotoba_or_5
     __kotoba_or_5
     (let
      [__kotoba_or_4 (string=? name (service-token-name))]
      (if
       __kotoba_or_4
       __kotoba_or_4
       (let
        [__kotoba_or_3 (string=? name (metrics-token-name))]
        (if
         __kotoba_or_3
         __kotoba_or_3
         (let
          [__kotoba_or_2 (string=? name (quic-cert-path-name))]
          (if
           __kotoba_or_2
           __kotoba_or_2
           (string=? name (quic-key-path-name)))))))))),
   :param-types [:string]}
  {:name env-for-secret-name,
   :params [name],
   :result :string,
   :effects #{},
   :body
   (if
    (string=? name (token-secret-name))
    (token-secret-env)
    (if
     (string=? name (service-token-name))
     (service-token-env)
     (if
      (string=? name (metrics-token-name))
      (metrics-token-env)
      (if
       (string=? name (quic-cert-path-name))
       (quic-cert-path-env)
       (if
        (string=? name (quic-key-path-name))
        (quic-key-path-env)
        ""))))),
   :param-types [:string]}
  {:name reply-tag,
   :params [kind],
   :result :string,
   :effects #{},
   :body
   (if
    (string=? kind (class-value))
    (class-value)
    (if
     (string=? kind (class-not-found))
     (class-not-found)
     (if
      (string=? kind (class-empty))
      (class-empty)
      (if
       (string=? kind (class-fetch))
       (class-fetch)
       (class-unknown))))),
   :param-types [:string]}
  {:name classify-fetched,
   :params [f],
   :result :string,
   :effects #{},
   :body
   (if
    (record-get
     [:record :secret/fetched [[:missing :bool] [:blank :bool]]]
     f
     :missing)
    (class-not-found)
    (if
     (record-get
      [:record :secret/fetched [[:missing :bool] [:blank :bool]]]
      f
      :blank)
     (class-empty)
     (class-value))),
   :param-types [[:ref :secret/fetched]]}
  {:name secret-error-code,
   :params [class],
   :result :string,
   :effects #{},
   :body
   (if
    (string=? class (class-empty))
    (string-concat (error-code-prefix) (class-empty))
    (if
     (string=? class (class-not-found))
     (string-concat (error-code-prefix) (class-not-found))
     (if
      (string=? class (class-fetch))
      (string-concat (error-code-prefix) (class-fetch))
      (string-concat (error-code-prefix) (class-unknown))))),
   :param-types [:string]}
  {:name secret-error-message,
   :params [class],
   :result :string,
   :effects #{},
   :body
   (if
    (string=? class (class-empty))
    (msg-empty)
    (if
     (string=? class (class-not-found))
     (msg-not-found)
     (if (string=? class (class-fetch)) (msg-fetch) (msg-unknown)))),
   :param-types [:string]}
  {:name reply-is-value?,
   :params [class],
   :result :bool,
   :effects #{},
   :body (string=? class (class-value)),
   :param-types [:string]}],
 :oracle-value nil,
 :schema-identities
 {:secret/fetched
  "0a81d05e65862ae5ca36e820c541c53e7825fcfe1eb8297c72a430525ef78645"},
 :exports
 [token-secret-name
  token-secret-env
  service-token-name
  service-token-env
  metrics-token-name
  metrics-token-env
  quic-cert-path-name
  quic-cert-path-env
  quic-key-path-name
  quic-key-path-env
  max-env-name
  max-path-ref
  blank?
  ws?
  valid-env-var-name?
  valid-path-ref-unix?
  env-for-secret-name
  known-secret-name?
  reply-tag
  classify-fetched
  secret-error-code
  secret-error-message
  reply-is-value?
  class-value
  class-not-found
  class-empty
  class-fetch
  class-unknown
  error-code-prefix
  msg-empty
  msg-not-found
  msg-fetch
  msg-unknown
  pem-begin-marker],
 :schemas
 {:secret/fetched
  [:record :secret/fetched [[:missing :bool] [:blank :bool]]]},
 :effects #{},
 :blocks [],
 :entry nil}
