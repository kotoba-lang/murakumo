{:format :kotoba.kir/v4,
 :signature nil,
 :functions
 [{:name blank?,
   :params [s],
   :result :i64,
   :effects #{},
   :body (if (string=? s "") 1 0),
   :param-types [:string]}
  {:name endpoint-kind,
   :params [endpoint],
   :result :string,
   :effects #{},
   :body
   (let
    [n (string-byte-length endpoint)]
    (if
     (if
      (< n 7)
      0
      (if (string=? (string-substring endpoint 0 7) "quic://") 1 0))
     "quic"
     (if
      (if
       (< n 9)
       0
       (if (string=? (string-substring endpoint 0 9) "webrtc://") 1 0))
      "webrtc"
      (if
       (if
        (< n 8)
        0
        (if (string=? (string-substring endpoint 0 8) "https://") 1 0))
       "webtransport"
       (if
        (if
         (< n 8)
         0
         (if
          (string=? (string-substring endpoint 0 8) "relay://")
          1
          0))
        "relay"
        "unknown"))))),
   :param-types [:string]}
  {:name option-name,
   :params [flag],
   :result :string,
   :effects #{},
   :body
   (let
    [n (string-byte-length flag)]
    (if
     (< n 2)
     ""
     (if
      (string=? (string-substring flag 0 2) "--")
      (string-substring flag 2 n)
      flag))),
   :param-types [:string]}
  {:name command-is-dial?,
   :params [command],
   :result :i64,
   :effects #{},
   :body (if (string=? command "dial") 1 0),
   :param-types [:string]}
  {:name dial-ok-reason,
   :params [is-dial missing-count],
   :result :string,
   :effects #{},
   :body
   (if
    (= is-dial 0)
    "unknown-command"
    (if (< 0 missing-count) "missing-options" "ok")),
   :param-types [:i64 :i64]}],
 :oracle-value nil,
 :schema-identities nil,
 :exports
 [endpoint-kind blank? option-name dial-ok-reason command-is-dial?],
 :schemas nil,
 :effects #{},
 :blocks [],
 :entry nil}
