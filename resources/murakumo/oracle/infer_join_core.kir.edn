{:format :kotoba.kir/v4,
 :signature nil,
 :functions
 [{:name tier-browser,
   :params [],
   :result :i64,
   :effects #{},
   :body 0,
   :param-types []}
  {:name tier-wasm,
   :params [],
   :result :i64,
   :effects #{},
   :body 1,
   :param-types []}
  {:name tier-native,
   :params [],
   :result :i64,
   :effects #{},
   :body 2,
   :param-types []}
  {:name max-resident-bytes,
   :params [tier],
   :result :i64,
   :effects #{},
   :body
   (if (= tier 0) 2147483648 (if (= tier 1) 4294967296 13958643712)),
   :param-types [:i64]}
  {:name needs-relay?,
   :params [tier inbound],
   :result :bool,
   :effects #{},
   :body (if (< tier 2) true (if inbound false true)),
   :param-types [:i64 :bool]}
  {:name swarm-can?,
   :params [kind],
   :result :bool,
   :effects #{},
   :body
   (if
    (string=? kind "media-postproc")
    true
    (if
     (string=? kind "small-shard")
     true
     (if
      (string=? kind "embarrassingly-parallel")
      true
      (if (string=? kind "prompt-eval") true false)))),
   :param-types [:string]}
  {:name native-can?,
   :params [kind],
   :result :bool,
   :effects #{},
   :body
   (if
    (string=? kind "host-large-model")
    true
    (if
     (string=? kind "low-latency-pipeline")
     true
     (if
      (string=? kind "media-generate")
      true
      (if (string=? kind "full-shard") true false)))),
   :param-types [:string]}
  {:name can?,
   :params [tier kind],
   :result :bool,
   :effects #{},
   :body (if (< tier 2) (swarm-can? kind) (native-can? kind)),
   :param-types [:i64 :string]}
  {:name clamp-resident,
   :params [mem tier-max],
   :result :i64,
   :effects #{},
   :body
   (let
    [__kotoba_binding-some_1 mem]
    (if
     (option-some?-of [:option :i64] __kotoba_binding-some_1)
     (let
      [m (option-value-of [:option :i64] __kotoba_binding-some_1 0)]
      (if (< m tier-max) m tier-max))
     tier-max)),
   :param-types [[:option :i64] :i64]}
  {:name eligible-for-work?,
   :params [can-kind max-res resident],
   :result :bool,
   :effects #{},
   :body (if can-kind (if (< max-res resident) false true) false),
   :param-types [:bool :i64 :i64]}],
 :oracle-value nil,
 :schema-identities nil,
 :exports
 [tier-browser
  tier-wasm
  tier-native
  max-resident-bytes
  needs-relay?
  can?
  clamp-resident
  eligible-for-work?],
 :schemas nil,
 :effects #{},
 :blocks [],
 :entry nil}
