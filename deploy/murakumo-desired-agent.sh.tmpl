#!/bin/sh
set -eu
cd __HOME__/.murakumo/desired-agent/repo
# Operator start is the desired guest, not clojure -M:desired.
# kekkai seal, mirror I/O, local apply, and runtime probe remain
# host-listen HOLD in this guest — this agent does not wrap a JVM alias.
exec kotoba run kotoba/desired.kotoba --function run --arg '"reconcile"'
